# Example configuration for libpillmom
# Copy this file to .env and fill in your actual values

# Database configuration (choose one)
#
# DATABASE_TYPE options:
#   - memory  : In-memory database (no persistence)
#   - local   : Local SQLite file database
#   - remote  : Remote Turso database
#   - turso   : Same as remote (for backwards compatibility)
#   - replica : Embedded replica (local with sync to remote)
#              Note: replica currently falls back to remote due to FFI issues

# Option 1: In-memory database (no persistence)
# DATABASE_TYPE=memory

# Option 2: Local SQLite database (default)
DATABASE_TYPE=local
DATABASE_PATH=example.db

# Option 3: Remote Turso database
# DATABASE_TYPE=remote
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token-here

# Option 4: Embedded replica (local with sync)
# DATABASE_TYPE=replica
# REPLICA_PATH=local_replica.db
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token-here
# SYNC_PERIOD=60  # Sync period in seconds (optional, default: 60)